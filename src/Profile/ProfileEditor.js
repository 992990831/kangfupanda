import React, { Component } from 'react';
import {SwipeAction, ListView, WingBlank, Button, InputItem, ImagePicker, Toast, Picker, List, DatePicker } from 'antd-mobile';
import { createForm } from 'rc-form';

const ProfileEditor = (props)=>{
    return(<div>
        ProfileEditor
    </div>)
}

export const ProfileEditorForm = createForm()(ProfileEditor);

